export enum CallDirection {
    INCOMING = 'INCOMING',
    OUTGOING = 'OUTGOING',
}

export enum CallStatus {
    PENDING = 'PENDING',
    COMPLETED = 'COMPLETED',
    MISSED = 'MISSED',
}

export enum UserRoles {
    ADMIN = 'ADMIN',
    RM = 'RM',
    STAFF = 'STAFF',
    MARKETING = 'MARKETING',
}
